telegram:
  bot_token: "your-telegram-bot-token-here"
  chat_id: "your-telegram-chat-id-here"

my_wallets:
  - "0xYourWalletAddress"

# Monitor settings, per-market overrides, and tracked accounts
# are all configurable via the web UI at http://localhost:8888
state_dir: "data"
web_port: 8888
